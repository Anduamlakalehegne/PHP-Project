const fname = document.getElementById('name');
const lname = document.getElementById('name');
const username = document.getElementById('name');
const email = document.getElementById('name');
const pnumber = document.getElementById('name');
const age = document.getElementById('name');
const region = document.getElementById('name');
const zone = document.getElementById('name');
const kebele = document.getElementById('name');

form.addEventListener('submit', (e) => {

    let messages = [];
    if (fname.value === "" || fname.value == null) {
        
    messages.push('Name Is Required');
    }
   
})